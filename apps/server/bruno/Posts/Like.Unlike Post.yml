info:
  name: Like.Unlike Post
  type: http
  seq: 7

http:
  method: POST
  url: "{{SERVER_URL}}/api/posts/:postId/toggle-like"
  params:
    - name: postId
      value: "36"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
