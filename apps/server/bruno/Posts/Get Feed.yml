info:
  name: Get Feed
  type: http
  seq: 7

http:
  method: GET
  url: "{{SERVER_URL}}/api/posts/feed"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
