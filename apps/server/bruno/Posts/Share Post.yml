info:
  name: Share Post
  type: http
  seq: 6

http:
  method: POST
  url: "{{SERVER_URL}}/api/posts/:postId/share"
  params:
    - name: postId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
