info:
  name: "Delete Post "
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{SERVER_URL}}/api/posts/:postId"
  params:
    - name: postId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
