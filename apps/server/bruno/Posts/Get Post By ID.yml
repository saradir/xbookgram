info:
  name: Get Post By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{SERVER_URL}}/api/posts/:postId"
  params:
    - name: postId
      value: "5"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
