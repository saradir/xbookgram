info:
  name: Get Notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_URL}}/api/notifications"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
