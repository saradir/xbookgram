info:
  name: Onboard User
  type: http
  seq: 3

http:
  method: POST
  url: "{{SERVER_URL}}/auth/onboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
