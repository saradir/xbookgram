info:
  name: Login with Google
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_URL}}/auth/google"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
