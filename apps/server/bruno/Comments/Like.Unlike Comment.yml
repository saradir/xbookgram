info:
  name: Like.Unlike Comment
  type: http
  seq: 4

http:
  method: POST
  url: "{{SERVER_URL}}/api/comments/:commentId/toggle-like"
  params:
    - name: commentId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
