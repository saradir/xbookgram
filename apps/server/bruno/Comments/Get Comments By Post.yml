info:
  name: Get Comments By Post
  type: http
  seq: 3

http:
  method: GET
  url: "{{SERVER_URL}}/api/posts/:postId/comments"
  params:
    - name: postId
      value: "36"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
