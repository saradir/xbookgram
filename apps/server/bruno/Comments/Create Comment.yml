info:
  name: Create Comment
  type: http
  seq: 1

http:
  method: POST
  url: "{{SERVER_URL}}/api/posts//:postId/comments"
  params:
    - name: postId
      value: "36"
      type: path
  body:
    type: json
    data: |-
      {
        "content": "hello"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
