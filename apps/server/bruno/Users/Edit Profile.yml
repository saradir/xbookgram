info:
  name: Edit Profile
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{SERVER_URL}}/api/users/profile"
  body:
    type: json
    data: |-
      {
        "name": "Ophir Golov"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
