info:
  name: Get User's Posts
  type: http
  seq: 5

http:
  method: GET
  url: "{{SERVER_URL}}/api/users/:userId/posts"
  params:
    - name: userId
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
