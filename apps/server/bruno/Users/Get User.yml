info:
  name: Get User
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_URL}}/api/users/:userId"
  params:
    - name: userId
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
