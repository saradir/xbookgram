info:
  name: Search Users
  type: http
  seq: 4

http:
  method: GET
  url: "{{SERVER_URL}}/api/users/search?q=oph"
  params:
    - name: q
      value: oph
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
