info:
  name: Toggle Follow
  type: http
  seq: 3

http:
  method: POST
  url: "{{SERVER_URL}}/api/users/:userId/toggle-follow"
  params:
    - name: userId
      value: "8"
      type: path
  body:
    type: json
    data: |-
      {
        "followedUserId": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
